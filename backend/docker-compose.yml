version: '3.8'

services:
  evolve-backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - JWT_SECRET=YmVzdC1rZXB0LXNlY3JldC1mb3Itand0LXRlc3RpbmctcHVycG9zZXM=
      - JWT_EXPIRATION=3600000
    #  - MAIL_HOST=smtp.gmail.com
    #  - MAIL_PORT=587
    #  - MAIL_USERNAME=
    #  - MAIL_PASSWORD=
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
